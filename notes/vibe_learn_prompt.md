## 整体
- 详细介绍 xxx.py

#  逐行
- 逐行讲解 xxx.py 每一段代码在做什么

## 学代码细节
- 详解 xxx ，数据结构和数据流要结合当前实际的例子(conda activate hkcodex)，例子要具体，以及为何这么设计 (数据结构，变量，策略逻辑等)

## git
- 检查现在的 git 暂存区的修改，仔细阅读相关代码，做宏观理解，仅写 message，不能提交；注意，需要要功能导向自然语言简洁描述，而非很多字段脚本的描述，可参考历史最近1次 (注意单次记录不止一行 要看单个完整的 message) 的提交 message 的写法思路，下一步优化方向 可参考你刚才的思路

## ETF轮动门控归因（只读）

```markdown
对 `目标路径信号目录` 做**最近 n 个交易日**的只读归因分析，结果直接输出到聊天框。

**要求：**
1. 读取同目录下的信号内容，以及上级目录的持仓记录；对照策略目录的 策略脚本 理解门控逻辑。
2. 分析每日因子得分排名（高分标的）、逐日门控拦截原因（策略相关），并说明最终为何 hold 或 rotation_switch 或止盈止损。
3. 可临时运行分析脚本复算原始因子与门控，但**不得修改、保存任何策略脚本或信号数据**。
4. 输出：n 日得分表、逐日门控明细表、门控贡献统计、综合结论；用中文，结构清晰。
5. 有 conda 环境你使用 conda env list 找到 hkcodex
```
